# BandKit backend

Staging backend API skeleton for BandKit.

This service is planned to expose `/api/v1/health` first, then PostgreSQL health, migrations, PermissionService and real API modules.

Current stage:

- no production data;
- no real database dependency yet;
- no frontend changes;
- intended for Ubuntu 22.04 staging VPS later.
